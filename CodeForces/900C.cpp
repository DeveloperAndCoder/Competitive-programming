#include<iostream>
#include<vector>

using namespace std;

int main()
{
    int n;
    cin >> n;
    int a[n];
    for(int i = 0; i < n; i++)
        cin >> a[i];
    vector<int> c;
    int j = 0;
    for(int i = 1; i < n; i++)
    {
        if(a[i]==a[i-1]+1)
        {
            c[]
        }
    }
    return 0;
}
